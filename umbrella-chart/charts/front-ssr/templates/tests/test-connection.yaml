apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "front-ssr.fullname" . }}-test-connection"
  labels:
    {{- include "front-ssr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "front-ssr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
